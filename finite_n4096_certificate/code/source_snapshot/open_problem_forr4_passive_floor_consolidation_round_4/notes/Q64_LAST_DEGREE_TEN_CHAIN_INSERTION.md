# Final degree-ten chain insertion at q=64

Date: 2026-07-16

Status: rigorous arbitrary-diagonal one-batch coefficient for the final four-entry degree-ten orbit in the live split-cubic/split-quintic family. This raises the q64 theorem count from 320 to 324. It does not prove the remaining 564 entries, intervalize the ledger, or establish the adaptive lift.

## Result

For the orbit generated by

$$
(3,1,1,5):(1,1,0,3),
$$

the arbitrary-law coefficient at $q=64$ is

$$
\boxed{0.0910312181521}.
$$

After insertion,

$$
\begin{aligned}
\beta&=0.746152924632,\\
P_{\rm Perron}&=0.307730226891,\\
P_{\rm promise}&=0.0170361000622,\\
P_{\rm total}&=0.324766326953.
\end{aligned}
$$

The routing margin is $0.00856700638000$, an improvement of $0.000517652654171$. Every remaining quintic entry now has total degree twelve. There are 104 such entries: 72 extreme and 32 balanced. Charging them at the existing local-slice values gives total $0.325037612931$ and leaves $0.00729572040278$ beyond the declared $10^{-3}$ allowance.

## Exact chain

Write the split cubic and quintic supports as

$$
C=\{x\}\mathbin{\dot\cup}E,
\qquad
S=F\mathbin{\dot\cup}G,
$$

with $|E|=|G|=2$ and $|F|=3$. Rows are $(x,a,F)$, columns are $(E,b,G)$, and the occurrence kernel is

$$
K=M_{31}(C,a)H_N(a,b)M_{15}(b,S).
$$

The proof completes each endpoint moment before restoring the two within-support distinctness masks. This avoids multiplying the loose endpoint slice energies.

## Cubic completion and mask

After extracting the endpoint Walsh phase, the completed cubic scalar kernel is a cross Gram of unit hidden-sign features. On the physical occurrence set it must also enforce $x\notin E$. For a fixed pair $E$, the centered-vector factorization of this mask gives

$$
\gamma_{N,2}
=1-{2\over N}
+\sqrt{2\left(1-{2\over N}\right)
              \left(1-{1\over N}\right)}.
$$

At $N=4096$,

$$
\gamma_{4096,2}=2.41320737011.
$$

Thus the completed split-cubic factor has trace-class Schur norm at most $\gamma_{N,2}$ for arbitrary diagonal laws.

## Transposed quintic completion

The quintic endpoint is the transpose of the completed pair/triple kernel used in the accepted internal-singleton theorem. Complete it to all pair/triple combinations. The completed kernel has Schur norm one. On overlaps, the repeated hidden signs cancel and leave cubic or singleton endpoint moments. The collision row has squared energy at most two, so subtracting it costs at most $\sqrt2$. Consequently

$$
\|B_{3|2}\|_{\rm Schur}
\le1+\sqrt2
=2.41421356237.
$$

Transpose does not change this trace-class multiplier bound.

## Walsh-chain collapse

The exact endpoint identity gives

$$
M_{31}(C,a)H_N(a,b)
={v_3(C)\over q}
H_N\bigl(a,\xi(C)\oplus b\bigr).
$$

After removing row-only and column-only characters, the residual is a repeated normalized Walsh matrix. Duplicate compression followed by Schatten Hölder gives coefficient one for that normalized matrix, leaving the explicit scalar factor $1/q$.

Multiplying the three independent Schur factors yields

$$
\boxed{
c(q)
\le {\gamma_{q^2,2}(1+\sqrt2)\over q}.
}
$$

At $q=64$, this is $0.0910312181521$.

The cubic and quintic completions share the same physical row and column laws only through Schur multiplication; neither law is optimized twice. The argument is therefore valid for arbitrary correlated diagonal laws, not only product or invariant laws.

## Regression scope

Exact $q=4$ and $q=8$ signed-permutation formulas stress random correlated-law submatrices of the full target tensor. A separate arbitrary-law test checks the explicit $1/q$ Walsh residual. The inherited regressions already verify the cubic distinctness factor and the completed-quintic collision bound.

Reproduce with:

    /opt/homebrew/Caskroom/miniconda/base/bin/python3 searches/q64_last_degree_ten_chain_insertion.py --write-artifact
    /opt/homebrew/Caskroom/miniconda/base/bin/python3 tests/q64_last_degree_ten_chain_insertion.py
